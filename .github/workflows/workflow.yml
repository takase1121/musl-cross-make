
name: build binaries

on:
  workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - run: sudo apt install build-essential
      - run: make -j$(nproc)
